// =============================================================================
// home_app_bar.dart
// =============================================================================
// WHAT: AppBar for HomeScreen — title "ARASH COURIER" + logout IconButton.
// INPUT: optional callback onLogoutConfirmed (or pass BuildContext and handle inside).
// OUTPUT: PreferredSizeWidget (AppBar).
// MOVE FROM: home_screen.dart — AppBar + logout AlertDialog block.
// =============================================================================
